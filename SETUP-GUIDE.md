# ナガシマヴィラ11 自社予約システム導入手順

## 先に確認する運用ルール

- 自社サイト：Stripe決済完了時に予約確定
- Airbnb：「今すぐ予約」をオフにして予約リクエスト制
- Airbnbの予約リクエストを承認する前に、Googleカレンダーを確認
- 自社予約が先に確定している場合はAirbnbを承認しない
- AirbnbのiCal取り込みには時間差があるため、Airbnbリクエストの自動承認は行わない

## 1. 予約管理用スプレッドシートを作る

1. ナガシマヴィラ11のGoogleアカウントでGoogleスプレッドシートを新規作成します。
2. ファイル名を「ナガシマヴィラ11 予約管理」にします。
3. タイムゾーンが日本になっていることを確認します。
4. 「拡張機能」→「Apps Script」を開きます。

## 2. Apps Scriptへファイルを登録する

`apps-script`フォルダ内の次のファイルを、同名でApps Scriptへ登録します。

- `Code.gs`
- `Booking.html`
- `Result.html`
- `appsscript.json`

`appsscript.json`を編集するには、Apps Scriptの「プロジェクトの設定」で「appsscript.json マニフェスト ファイルをエディタで表示する」をオンにします。

## 3. 初期設定を実行する

1. Apps Script上部の関数選択で `setupNagashimaVilla11` を選択します。
2. 「実行」を押します。
3. Googleから確認画面が出たら、ナガシマヴィラ11のアカウントで権限を承認します。
4. スプレッドシートへ戻り、次の3シートが作られたことを確認します。

   - 基本設定
   - 日別料金
   - 予約一覧

5. Googleカレンダーに「ナガシマヴィラ11｜自社予約・販売停止」が作成されたことを確認します。

## 4. Airbnbカレンダーを設定する

### AirbnbからURLを取得

1. Airbnbのリスティングカレンダーを開きます。
2. 「予約可能状況」→「カレンダーを連携する」を開きます。
3. Airbnbカレンダーをエクスポートし、`.ics`のURLをコピーします。

### Apps Scriptへ登録

1. Apps Scriptの「プロジェクトの設定」を開きます。
2. 「スクリプト プロパティ」に次を追加します。

| プロパティ | 値 |
| --- | --- |
| `AIRBNB_ICAL_URL` | Airbnbからコピーした`.ics` URL |

このURLは予約情報にアクセスできる秘密情報です。HTMLやGitHubへ書かないでください。

### 自社予約カレンダーをAirbnbへ登録

1. Googleカレンダーで「ナガシマヴィラ11｜自社予約・販売停止」の設定を開きます。
2. 「カレンダーの統合」にある「iCal形式の非公開URL」をコピーします。
3. Airbnbの「カレンダーをインポート」へ貼り付けます。
4. カレンダー名を「自社予約」にします。

## 5. Stripeをテストモードで設定する

最初は必ずStripeのテストモードで確認してください。

1. Stripeダッシュボードでテストモードをオンにします。
2. 開発者向け画面からテスト用シークレットキーを取得します。
3. Apps Scriptのスクリプトプロパティへ追加します。

| プロパティ | 値 |
| --- | --- |
| `STRIPE_SECRET_KEY` | `sk_test_...`から始まるテスト用キー |

シークレットキーはメール・チャット・GitHubへ記載しないでください。

## 6. Webアプリとして公開する

1. Apps Script右上の「デプロイ」→「新しいデプロイ」を選択します。
2. 種類は「ウェブアプリ」を選択します。
3. 実行するユーザーは「自分」にします。
4. アクセスできるユーザーは「全員」にします。
5. デプロイして、末尾が `/exec` のURLをコピーします。
6. Apps Scriptのスクリプトプロパティへ追加します。

| プロパティ | 値 |
| --- | --- |
| `WEB_APP_URL` | コピーした `/exec` URL |

## 7. Stripe webhookを設定する

決済後にお客様が完了画面を閉じても予約を確定させるために必要です。

1. Apps Scriptのスクリプトプロパティで、自動作成された `STRIPE_WEBHOOK_TOKEN` の値を確認します。
2. Stripeの「開発者」→「Webhook」で送信先を追加します。
3. 送信先URLを次の形式にします。

```text
WEB_APP_URL?stripe_webhook=STRIPE_WEBHOOK_TOKEN
```

4. 受信イベントは `checkout.session.completed` のみを選びます。

## 8. 公式サイトへ予約フォームを接続する

`booking-config.js`を開き、Apps ScriptのWebアプリURLを設定します。

```javascript
window.NV11_BOOKING_APP_URL='https://script.google.com/macros/s/...../exec';
```

設定後、サイト一式をGitHub Pagesのリポジトリへアップロードします。

## 9. 日別料金を設定する

通常料金は自動計算されます。

- 月～木：30,000円
- 金～日：36,000円
- 6人目以降：1名1泊3,000円
- 清掃費：1予約3,000円

大型連休等は「日別料金」シートへ入力します。

| 日付 | 上書き料金 | 倍率 | 販売停止 | 最低泊数 | 名称・メモ |
| --- | ---: | ---: | --- | ---: | --- |
| 2026/12/29 | 45,000 |  | FALSE | 2 | 年末年始 |
| 2026/12/30 |  | 1.5 | FALSE | 2 | 年末年始 |
| 2027/01/01 |  |  | TRUE |  | 休館日 |

「上書き料金」がある場合は、その金額を最優先します。倍率を使う場合は、曜日加算ではなく基本料金30,000円に対する倍率になります。

## 10. 公開前テスト

1. 平日1泊・5名で合計33,000円になることを確認します。
2. 金土日1泊・5名で合計39,000円になることを確認します。
3. 平日1泊・6名で合計36,000円になることを確認します。
4. 11名まで予約でき、12名は選択できないことを確認します。
5. 当日12:00以降に当日予約ができないことを確認します。
6. 日別料金の上書きが反映されることを確認します。
7. Airbnb予約済み日が自社サイトで拒否されることを確認します。
8. Stripeテストカード `4242 4242 4242 4242` で決済します。
9. 決済後、「予約一覧」が予約確定になることを確認します。
10. Googleカレンダーに自社予約が登録されることを確認します。
11. お客様と管理者に予約確定メールが届くことを確認します。
12. Airbnb側で自社予約日がブロックされることを確認します。

すべて確認できたら、`STRIPE_SECRET_KEY`を本番用の `sk_live_...` に差し替え、Stripe webhookも本番モードで作成します。

## 現段階で手動となる業務

- 予約キャンセルと返金
- キャンセル料の計算・徴収
- Airbnb予約リクエストの承認
- Airbnb予約者への連絡
- TOMARO＋への予約情報登録または連携
- スマートロック暗証番号の発行確認

## 重要事項

AirbnbのiCal同期には時間差があります。Airbnbは予約リクエスト制で運用し、承認前に必ずGoogleカレンダーと予約一覧を確認してください。独自システムとiCalだけで、ダブルブッキングを完全にゼロにすることはできません。
